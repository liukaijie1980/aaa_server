# Cursor 忽略文件 - 排除已完成的模块以降低上下文消耗

# 排除输出文件

docker/nginx/app/
docker/java/app/
docker/java/api/
# 排除虚拟环境
**/venv/
**/__pycache__/
**/*.pyc

# 排除 node_modules
**/node_modules/

# 排除上传目录和临时文件

*.log

# 排除编译产物
**/*.pyc
**/*.pyo
**/*.pyd
**/*.so
**/*.dll
**/*.dylib

# 排除系统文件
.DS_Store
Thumbs.db
desktop.ini

